export const navList = state => state.navList;

export const currentName = state => state.currentName;

export const editBlog = state => state.editBlog;

export const draftCount = state => state.draftCount;

export const bbsCount = state => state.bbsCount;

export const onlineArticleCount = state => state.onlineArticleCount;

export const usersCount = state => state.usersCount;

export const walkingBlogCount = state => state.walkingBlogCount;